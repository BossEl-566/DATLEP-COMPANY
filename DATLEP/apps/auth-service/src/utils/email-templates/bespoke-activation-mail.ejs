<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        background-color: #f9f9f9;
        padding: 20px;
      }

      .container {
        max-width: 600px;
        margin: 0 auto;
        background-color: #fff;
        padding: 30px;
        border-radius: 8px;
        box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      }

      h1 {
        color: #4CAF50;
        font-size: 28px;
        margin-bottom: 10px;
      }

      p {
        font-size: 16px;
      }

      a {
        color: #1E90FF;
        text-decoration: none;
      }

      a:hover {
        text-decoration: underline;
      }

      .otp-box {
        margin: 20px 0;
        padding: 15px;
        background: #f4f4f4;
        border: 1px dashed #4CAF50;
        font-size: 24px;
        font-weight: bold;
        text-align: center;
        letter-spacing: 4px;
      }

      .activation-link {
        display: inline-block;
        margin-top: 15px;
        padding: 10px 20px;
        background-color: #4CAF50;
        color: #fff;
        font-weight: bold;
        text-decoration: none;
        border-radius: 5px;
      }

      .activation-link:hover {
        background-color: #45a049;
      }

      .footer {
        margin-top: 30px;
        font-size: 14px;
        color: #777;
      }
    </style>
    <title>Seller Activation</title>
  </head>
  <body>
    <div class="container">
      <h1>Welcome to DATLEP, <%= name %>!</h1>

      <p>Thank you for registering as a creator. To activate your creator account, please use the One-Time Password (OTP) below:</p>

      <div class="otp-box">
        <%= otp %>
      </div>

      <p>This OTP is valid for <strong>5 minutes</strong>.</p>

      <p>Or click the button below to activate your account immediately:</p>

      <a class="activation-link" href="<%= activationLink %>">Activate Your Seller Account</a>

      <div class="footer">
        <p>If you did not sign up for this account, please ignore this email.</p>
        <p>Best regards,<br />The DATLEP Team</p>
      </div>
    </div>
  </body>
</html>
